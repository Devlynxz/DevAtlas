{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "preDeployCommand": ["alembic upgrade head", "python seed_data.py"],
    "restartPolicyType": "ON_FAILURE"
  }
}
